# Stage 04 COMSOL 电热场阶段报告

## 判定

`smoke_pass + mesh_pass`。真实 LIGGGHTS 直接接触网络已经经过属性映射进入 COMSOL 6.4，
Electric Currents、Heat Transfer 和电磁热源完成稳态耦合求解，并由独立有限体积
实现通过能量与空间场交叉验证。

## 求解证据

- COMSOL 6.4.0.293 批处理运行时间 22 秒。
- 网格包含 3316 个三角形单元，最小单元质量 0.6652。
- 求解 13570 个自由度和 308 个内部自由度。
- 非线性求解器在 4 次迭代后结束，最终 `SolEst=1e-16`、`ResEst=0.00098`。
- 导出 1735 个场节点，电势范围为 `0-0.1 V`，温度范围为
  `293.15-293.788827 K`。

## 跨求解器结果

| 指标 | COMSOL 6.4 | 开源 FVM | 相对差 |
|---|---:|---:|---:|
| Joule 功率 (W/m depth) | 396.328061 | 391.593371 | 1.202% |
| 最大温升 (K) | 0.639814 | 0.636193 | 0.568% |

- 电势场归一化 RMSE：`0.002930`。
- 温度场归一化 RMSE：`0.007652`。
- FVM 离散电功率守恒误差：`7.26e-16`。
- 九项 smoke gate 全部通过。
- GitHub Actions run `28793218330` 在 Ubuntu 上重建输入并重跑 FVM，27 秒完成且
  状态为 `success`；artifact digest 为 `sha256:2199a770...b6686`。

对比图见 `evidence/comparison/comsol_fvm_comparison.png`，机器判定见
`evidence/comparison/comparison_summary.json`。

## 网格无关性

保持 DEM 接触输入、`81 x 43` 属性网格、电压、温度边界和材料场不变，只改变
COMSOL 自由三角形网格：

| 网格 | hmax/hmin (um) | 单元数 | 自由度 | 最小质量 | Joule 功率 (W/m) | 最大温升 (K) |
|---|---:|---:|---:|---:|---:|---:|
| coarse | 12/1.5 | 1512 | 6258 | 0.7193 | 396.307677 | 0.640089 |
| medium | 8/1 | 3316 | 13570 | 0.6652 | 396.328061 | 0.639814 |
| fine | 5/0.625 | 8390 | 34050 | 0.6694 | 396.218160 | 0.639695 |

中/细网格的 Joule 功率相对差为 `0.0277%`，最大温升相对差为 `0.0187%`；
粗/细网格对应差值也只有 `0.0226%` 和 `0.0616%`。十项网格门槛全部通过，
原 3316 单元中网格对这两个全局量已足够。

证据见 `evidence/mesh_convergence/comparison/`。该结论只证明固定 smoke 模型的
离散收敛，不代表电导率、热导率或接触电阻已经物理标定。

## 科学含义

接触力较强、接触更密集的位置形成更高的均匀化电导率，电流密度和 Joule 热点
随之出现；温度场经过热传导平滑后仍保留热点空间差异。这验证了“应力/接触网络
影响电流分布，再影响温度场”的可执行链条。

这里的电热参数是 smoke 参数，不能直接解释为实际 Al-diamond 合金温升。网格误差
已被排除为当前主要风险；论文级复现仍需标定接触电阻、材料温度依赖性、颗粒几何
和加载电流。
