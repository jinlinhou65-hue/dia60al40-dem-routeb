# GitHub run 29598918283 failure evidence

Status: `failure_preserved`  
Workflow: `al-oxide-network-verification`  
Commit: `67602cc50426e98fe76b1068295b93db4b694e70`  
Run: `29598918283`  
Job: `oxide-network`, job ID `87946033168`  
Duration: 2026-07-17 17:08:59Z to 17:09:13Z

## Failure location

Checkout and pinned dependency installation succeeded. Step 4,
`Compile and test the preregistered solver`, failed after compilation while running the two
targeted unittest modules. Nine tests ran; two ended in error. The recomputation, reference
comparison and artifact upload steps were skipped, so this run produced no scientific result
artifact.

Both errors stopped in `verify_input_contract` before the KCL network solve:

```text
pilot_final_particles.csv observed SHA-256:
d66af5d5e4a70b33ba91790ad8907a5e9637ce88f5039c97361f98a09cd0d089

preregistered Windows raw-byte SHA-256:
a65ce68b8f407b15a28fa4b307ddf76f655b4811fb4ad3d7696cb580eb5deaf2
```

The observed value is exactly the SHA-256 obtained after changing only CRLF/CR line endings to
LF. The same condition applies to the other two frozen CSV inputs, although execution stopped on
the first file.

## Corrective boundary

The repair may add canonical-LF hashes while retaining all original raw-byte hashes. It must not
change archived CSV values, contact counts, graph topology, oxide equations, the 18 scenarios or
the eight preregistered scientific gates. Tests must accept equivalent line-ending encodings and
still reject any content change before solution.

This failure proves a cross-platform packaging defect only. It neither contradicts nor verifies
the local oxide-network numerical result.
